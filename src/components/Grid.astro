<section class="grid" aria-hidden="true">
  <div class="grid-bkg"></div>
  <div class="grid-line line-d1"></div>
  <div class="grid-line line-d2"></div>
  <div class="grid-line line-v1"></div>
  <div class="grid-line line-v2"></div>
</section>
<div class="grid-nav-bkg tt"></div>

<style>
	.grid {
		display: grid;
		grid-column: 1 / -1;
		grid-row: 1 / -1;
		grid-template-columns: subgrid;
		grid-template-rows: subgrid;
		z-index: -1;
		pointer-events: none;
	}
	.grid-line {
		background-color: var(--accent-dark);
	}
	.grid-line.line-v1,
	.grid-line.line-v2 {
		grid-column: 2;
		grid-row: 1 / -1;
		width: 1px;
		transform: translateX(-50%);
	}
	.grid-line.line-v2 {
		grid-column: 3;
	}

	.grid-line.line-d1,
	.grid-line.line-d2,
	.grid-bkg {
		grid-column: 1 / -1;
		grid-row: 3;
		height: 1px;
		transform: skewY(30deg);
		transform-origin: calc((100% - min(var(--width), 90%)) / 2) 100%;
	}
	.grid-line.line-d2 {
		transform: skewY(-30deg);
	}

	.grid-bkg {
		grid-row: 1 / 3;
		background-color: var(--background);
		height: 100%;
    transform: skewY(30deg) scaleY(9);
	}

	.grid-nav-bkg {
		grid-column: 2 / 3;
		grid-row: 2 / 4;
		position: sticky;
		top: 0;
		background-color: var(--accent);
		z-index: 1;
		filter: blur(0.3rem);
		border-radius: 2em;
		max-width: 17.5rem;
		margin-inline-start: 2.1em;
		animation-name: grid-nav-bkg;
		animation-timing-function: ease-in;
		--to: 50rem;
		mix-blend-mode: lighten;
}
@media (prefers-color-scheme: dark) {
	.grid-nav-bkg {
		mix-blend-mode: darken;
	}
}
@keyframes grid-nav-bkg {
	0% {
		font-size: 0.25rem;
	}
	100% {
		font-size: 1rem;
	}
}
</style>