---
import Logo from '../components/Logo.astro';
import IconMastodon from '../components/IconMastodon.astro';
import IconGithub from './IconGithub.astro';
---

<nav class="nav wrapper">
  <div class="content">
    <a href="/" class="logo tt">
      <Logo />
      <span class="hidden">Typetura</span>
    </a>
    <a href="mailto:scott@scottkellum.com">Get in touch</a>
    <a href="https://calendly.com/typetura/30min">Book a call</a>
		<div class="social-container">
			<div class="social">
				<a href="https://typetura.social/@scottkellum" target="_blank" rel="noopener noreferrer"><IconMastodon /><span class="hidden">Mastodon</span></a>
				<a href="https://www.instagram.com/typeturadesign/" target="_blank" rel="noopener noreferrer"><IconGithub /><span class="hidden">Github</span></a>
			</div>
		</div>
  </div>
</nav>

<style is:global>
  .nav {
		position: sticky;
		top: 0;
		mix-blend-mode: var(--blend);
		z-index: 2;
	}
	.nav > .content {
		display: flex;
		gap: 1rem;
		height: 2rem;
		align-items: center;
		font-stretch: expanded;
		color: var(--primary-dark);
		animation-name: nav-content;
		animation-timing-function: ease-in;
		font-family: var(--display);
		font-stretch: 160%;
		font-weight: 400;
		--to: 50rem;
	}
	@keyframes nav-content {
		0% {
			font-size: 0.25rem;
		}
		100% {
			font-size: 1rem;
		}
	}
	.social-container {
		height: 1rem;
	}
	.nav .social {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 0.5rem;
		align-items: center;
		color: var(--primary);
		height: 1rem;
		transition: all 0.5s;
	}
  .social a:hover {
    color: var(--action);
  }
	.nav a.logo {
		margin-left: -0.5em;
		font-size: 3em;
		display: block;
		width: 1em;
		aspect-ratio: 1;
		color: var(--primary);
		animation-name: logo;
		animation-timing-function: ease-in;
		--to: 50rem;
	}
	.nav svg.logo {
		width: 100%;
		height: 100%;
	}
	@keyframes logo {
		0% {
			font-size: 1rem;
		}
		100% {
			font-size: 4rem;
		}
	}
	.nav a {
		text-decoration: none;
		text-wrap: balance;
		line-height: 1;
	}
	.nav a:hover {
		text-decoration: underline;
	}
</style>