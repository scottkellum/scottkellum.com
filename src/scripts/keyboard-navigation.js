// DO THIS
// Container.scrollBy({ top: 0, left: 1, behavior: 'smooth' });

// document.addEventListener("keydown", (e) => {
//   if (e.key !== "ArrowDown" && e.key !== "ArrowUp") return;
//   e.preventDefault();

//   if (e.key === "ArrowDown") {
//     const next = positions.find((p) => p > current + threshold);
//     if (next !== undefined) window.scrollTo({ left: next });
//   } else {
//     const prev = [...positions].reverse().find((p) => p < current - threshold);
//     if (prev !== undefined) window.scrollTo({ left: prev });
//   }
// });
